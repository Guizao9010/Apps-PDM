import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  valor: {
    fontSize: 100,
    textAlign: 'center',
    borderWidth: 1,
    borderColor: '#222',
    margin: 20,
    padding: 50,
  },
  titulo: {
    fontSize: 20,
    fontFamily: 'Comic Sans MS',
    fontWeight: 'normal',
    textAlign: 'center',
  },
})

export {styles}